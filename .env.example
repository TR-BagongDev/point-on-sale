# ============================================
# Point on Sale - Environment Configuration Template
# ============================================
# Copy this file to .env and fill in the actual values

# --------------------------------------------
# Database (PostgreSQL) - Prisma 7 Configuration
# --------------------------------------------
# Prisma Accelerate URL (serverless connection for production/edge runtime)
# Get this from: https://console.prisma.io/workspace/<workspace>/projects/<project>/accelerate
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_ACCELERATE_API_KEY"

# Direct PostgreSQL connection (required for migrations/db push)
# Get this from Prisma Console > Project > Connection string (with &pool=true)
DIRECT_URL="postgres://project-id:api-key@db.prisma.io:5432/postgres?sslmode=require&pool=true"

# --------------------------------------------
# NextAuth.js Authentication
# --------------------------------------------
NEXTAUTH_URL="http://localhost:3000"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-secure-secret-here"

# --------------------------------------------
# Application Environment
# --------------------------------------------
NODE_ENV="development"

# --------------------------------------------
# Optional Configuration
# --------------------------------------------
# NEXT_PUBLIC_APP_URL="http://localhost:3000"
# ENABLE_OFFLINE_MODE="true"
# ENABLE_ANALYTICS="true"
